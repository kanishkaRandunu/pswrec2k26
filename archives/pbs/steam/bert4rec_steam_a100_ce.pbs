#!/bin/bash
#PBS -P up63
#PBS -q dgxa100
#PBS -l ncpus=16
#PBS -l mem=16GB
#PBS -l ngpus=1
#PBS -l walltime=06:00:00
#PBS -l storage=scratch/up63+gdata/up63
#PBS -l wd
#PBS -j oe
#PBS -N bert4rec_steam_a100_ce

# BERT4Rec on Steam (duplicate removal) on A100 (CE config: configs/steam/steam_BERT4Rec.yaml).
# Submit from repo root: qsub pbs/steam/bert4rec_steam_a100_ce.pbs

set -e
cd "$PBS_O_WORKDIR"

echo "=== hostname ==="
hostname
echo "=== PBS_JOBID ==="
echo "$PBS_JOBID"

module purge
module load cuda/12.9.0

source /scratch/up63/kd6504/venvs/hopper-cu124/bin/activate
mkdir -p log/BERT4Rec
echo "$(date '+%Y-%m-%d %H:%M:%S')" > "log/BERT4Rec/.job_${PBS_JOBID}.started"

# Live log (follow with: tail -f bert4rec_steam_live_<JOBID>.log)
LIVE_LOG="bert4rec_steam_live_${PBS_JOBID}.log"
python -u RecBole/run_recbole.py -m BERT4Rec -d steam-duprem --config_files configs/steam/steam_BERT4Rec.yaml 2>&1 | tee "${LIVE_LOG}"

echo "Job completed."
