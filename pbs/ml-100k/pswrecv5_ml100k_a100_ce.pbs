#!/bin/bash
#PBS -P up63
#PBS -q dgxa100
#PBS -l ncpus=16
#PBS -l mem=16GB
#PBS -l ngpus=1
#PBS -l walltime=04:00:00
#PBS -l storage=scratch/up63+gdata/up63
#PBS -l wd
#PBS -j oe
#PBS -N pswrecv5_ml100k_a100_ce

# PSWRec (pswrecv5) on ML-100K (benchmark). Submit from repo root: qsub pbs/ml-100k/pswrecv5_ml100k_a100_ce.pbs

set -e
cd "$PBS_O_WORKDIR"

echo "=== hostname ==="
hostname
echo "=== PBS_JOBID ==="
echo "$PBS_JOBID"

module purge
module load cuda/12.9.0

source /scratch/up63/kd6504/venvs/hopper-cu124/bin/activate
mkdir -p log/pswrecv5
echo "$(date '+%Y-%m-%d %H:%M:%S')" > "log/pswrecv5/.job_${PBS_JOBID}.started"

LIVE_LOG="pswrecv5_ml100k_live_${PBS_JOBID}.log"
python -u RecBole/run_recbole.py -m pswrecv5 -d ml-100k --config_files configs/ml-100k/ml100k_pswrecv5.yaml 2>&1 | tee "${LIVE_LOG}"

echo "Job completed."
